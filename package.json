{
  "name": "zenn",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "zenn preview",
    "new:article": "bin/new.sh",
    "deploy": "git add . && git commit -m '[auto] upd article & books' && git push origin HEAD",
    "lint": "prettier './**/*.{json,md}' --check",
    "fix": "prettier './**/*.{json,md}' --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "zenn-cli": "^0.1.153"
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
